type: object
properties:
  access_token:
    $ref: "/schema/access-token.yaml"
  refresh_token:
    $ref: "/schema/refresh-token.yaml"
  success:
    type: boolean
    exmaple: true
required:
  - access_token
  - refresh_token
  - success